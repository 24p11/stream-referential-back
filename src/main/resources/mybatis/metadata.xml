<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fr.aphp.referential.load.mybatis.Metadata">
    <update id="updateMetadataEndDate">
        UPDATE metadata m
        JOIN concept r ON r.vocabulary_id_concept_code = m.concept_id
        SET m.end_date = r.end_date
        WHERE r.`vocabulary_id` = #{vocabularyId}
    </update>

    <!-- UPSERT -->
    <insert id="upsertMetadata">
        INSERT INTO metadata (`concept_id`, `name`, `value`, `standard_concept`, `start_date`) VALUES
        <foreach item="metadata" collection="list" separator=",">
            (
            #{metadata.conceptId},
            #{metadata.name},
            #{metadata.value},
            #{metadata.standardConcept},
            #{metadata.startDate}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        `value`=VALUES(`value`);
    </insert>
</mapper>
